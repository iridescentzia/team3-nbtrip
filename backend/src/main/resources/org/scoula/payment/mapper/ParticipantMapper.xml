<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "--//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.scoula.payment.mapper.ParticipantMapper">
    <!-- 특정 paymentId로 결제 참여자 리스트 조회 -->
    <select id="findByPaymentId" resultType="org.scoula.payment.domain.ParticipantVO">
        SELECT participant_id, payment_id, user_id, split_amount
        FROM payment_participant
        WHERE payment_id = #{paymentId}
    </select>

    <!-- 결제 참여자 단건 등록 -->
    <insert id="insertParticipant" parameterType="org.scoula.payment.domain.ParticipantVO">
        INSERT INTO payment_participant (payment_id, user_id, split_amount)
        VALUES (#{paymentId}, #{userId}, #{splitAmount})
    </insert>

    <!-- 결제 참여자 다수 등록 -->
    <insert id="insertParticipants">
        INSERT INTO payment_participant (payment_id, user_id, split_amount)
        VALUES
            <foreach collection="participants" item="participant" separator=",">
                (#{participant.paymentId}, #{participant.userId}, #{participant.splitAmount})
            </foreach>
    </insert>

    <!-- 결제 참여자 분배 금액 수정 -->
    <update id="updateAmount">
        UPDATE payment_participant
        SET split_amount = #{splitAmount}
        WHERE payment_id = #{paymentId}
        AND user_id = #{userId}
    </update>

    <!-- 특정 결제 참여자 삭제 -->
    <delete id="deleteParticipant">
        DELETE FROM payment_participant
        WHERE payment_id = #{paymentId} AND user_id = #{userId}
    </delete>

    <!--  특정 결제의 모든 참여자 삭제  -->
    <delete id="deleteParticipantsByPaymentId">
        DELETE FROM payment_participant
        WHERE payment_id = #{paymentId}
    </delete>
</mapper>
